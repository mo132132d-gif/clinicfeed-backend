const supplierStatuses = ['Active', 'Pending', 'Suspended', 'Inactive', 'Blacklisted'];
const contractStatuses = ['Active', 'Expired', 'Terminated'];
const documentTypes = ['CR', 'VAT', 'Authorization', 'Catalog', 'Price List'];
const roles = ['admin', 'operations', 'sales', 'viewer'];

module.exports = {
  supplierStatuses,
  contractStatuses,
  documentTypes,
  roles
};
